namespace MASFoundation
{
    /// <summary>
    /// HTTP request body type
    /// </summary>
    public enum RequestType
    {
        None,
        Json,
        ScimJson,
        PlainText,
        FormUrlEncoded,
        Xml
    }
}
